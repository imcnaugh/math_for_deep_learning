# Math for deep learning

This repo will hold the notes and code I will produce as I read the book.

For starters, I made sure python was installed and a few pip packages were installed

```bash
python3 --version
pip3 install numpy --user
pip3 install scipy --user
pip3 install matplotlib --user
pip3 install scikit-learn --user
```

